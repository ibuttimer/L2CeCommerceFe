import { AuthenticationStatus } from './authentication-status';

describe('AuthenticationStatus', () => {
  it('should create an instance', () => {
    expect(new AuthenticationStatus()).toBeTruthy();
  });
});
